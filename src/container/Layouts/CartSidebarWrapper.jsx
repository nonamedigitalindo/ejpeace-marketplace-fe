import CartSidebar from "./CartSidebar";

// This wrapper ensures the CartSidebar is always mounted but only visible when needed
export default function CartSidebarWrapper() {
  return <CartSidebar />;
}
